# Deprecated Documents

This directory contains earlier planning notes and prototype-era design documents.
They are retained for historical context only.

Use the current HLBench documentation from the top-level `docs/README.md` and
the protocol-specific directories under `docs/`.
