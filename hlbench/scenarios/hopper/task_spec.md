# Hopper

This file is scenario-author reference text. Generated workspaces receive
`task.md` and `task_contract.json`.

Hop forward without falling. Return three continuous actions:
`[thigh_torque, leg_torque, foot_torque]`, each in `[-1, 1]`.
