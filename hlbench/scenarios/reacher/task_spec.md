# Reacher

This file is scenario-author reference text. Generated workspaces receive
`task.md` and `task_contract.json`.

Move the fingertip toward the target while minimizing control effort. Return two
continuous actions: `[shoulder_torque, elbow_torque]`, each in `[-1, 1]`.
