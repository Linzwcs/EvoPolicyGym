"""hlbench — Benchmark for iterative policy synthesis under tight rollout budgets.

Public API (lib mode, for tests and tooling — agents use HTTP):

    from hlbench.core.server import Server

For agents, the canonical interface is HTTP. See AGENT.md §4.0.
"""

__version__ = "0.1.0a0"
