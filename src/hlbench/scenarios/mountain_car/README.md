# Mountain Car Scenario

Smoke scenario for a discrete-action Gymnasium environment with dense negative
per-step reward.

The policy-visible observation is the official Gymnasium Classic Control
telemetry vector: `[position, velocity]`.
